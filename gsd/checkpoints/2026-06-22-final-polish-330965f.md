# GSD Checkpoint — ADR-0016 Phase 1 final polish at 330965f

Date: 2026-06-22
Project: x0x
Slice/question: Phase 1 small final polish before PR decision
Prepared by: orchestrator
Agents/tools used: local cargo/gh, `codereviewer`, `verifier`, `craft`, `adversarial`, `cleancontext`

## Status

Done for final-polish implementation / Phase 1 remains feature-complete / PR not opened.

Meaningful work-unit? Yes — security hardening on a terminality/card-protection gate in shared Rust code.
Review cadence: completed for this work-unit.
Unreviewed backlog if deferred: none for this polish. Existing Phase 2 deferral remains the card-bound discovery/request-access authority-seeding limitation recorded in `gsd/plan/phase-1-pr-notes.md`.

## What happened

- Build head advanced from `1c3f17a58c94c04f4099014bfad121f04dc1b904` to `330965f411f17ab328c68ca9b39dfa46bfa6d5b5`.
- Commit: `330965f fix(adr-0016-phase-1): fail closed terminal card guard`.
- Change set is limited to `src/bin/x0xd.rs`.
- Implemented approved items:
  - `local_group_has_protected_crypto_material` now fails closed for TreeKEM snapshot/journal `try_exists(...)` IO errors via `unwrap_or(true)`.
  - Added readable section/group comments around the named-group terminality helper cluster.
  - Deduplicated the two byte-identical test-only force hooks through a shared `#[cfg(test)]` helper without holding the `StdMutex` across `.await`.
  - Added focused regression `withdrawn_card_protected_crypto_probe_fails_closed_on_io_error`.
- Pushed only to Jim's fork branch `feat/adr-0016-phase-1-authority-alignment`.
- No PR opened.

## Evidence

CI arbiter / green of record:

- Location: draft mirror PR #5, <https://github.com/JimCollinson/x0x/pull/5>
- Status: raw red; internally accepted under `gsd/ci-arbiter.md` startup-timeout carve-out. Do **not** call raw CI green.
- Passing checks after `330965f`: Format Check, Clippy Lint, Documentation, Property Tests, API + GUI Parity Gate, API Coverage Guard, Coverage Gate, all Build jobs, Cargo Audit, Panic Scanner, Validate release metadata. Soak Test skipped.
- Red checks after `330965f`:
  - CI run `27983463441`, job `82819093492` (`Test Suite`): `x0x::named_group_join_metadata_event forged_member_joined_admin_role_or_secret_is_rejected` failed at `tests/harness/src/cluster.rs:68:17`, `x0xd pair-alice-11121 did not become healthy within 90s`; summary 1682 passed, 1 failed, 164 skipped, 88 not run due fail-fast.
  - Integration & Soak run `27983463424`, job `82819093185` (`Multi-Agent Integration`): `x0x::named_group_integration named_group_admin_disband_propagates_to_peer_after_creator_delete_409` failed at `tests/harness/src/cluster.rs:68:17`, `x0xd pair-alice-57771 did not become healthy within 90s`; summary 2 passed, 1 failed, 24 not run due fail-fast.
- Carve-out classification:
  - Signature: both failures are pre-assertion daemon-startup health timeouts at harness bring-up.
  - Isolation: 2 timed-out tests total, within `<= 3`; all other non-skipped checks passed.
  - Diff guard: polish diff touches only `src/bin/x0xd.rs` terminality helpers/tests. It does not touch `tests/harness/**`, CI, `.gsd/gate.sh`, service wrappers, build invocation, environment setup, `Cargo.toml`, `Cargo.lock`, `fn main`, `/health`, startup, transport, networking, bootstrap, or presence code.
  - Baseline: previous Phase 1 integrated checkpoint records base/equivalent vs branch reproductions for the same recurring CI failure surfaces, all failing before assertions at `tests/harness/src/cluster.rs:68:17` with 90s startup-health timeouts.

Local fast gate / `.gsd.gate.sh`:

- Pre-push hook on push to Jim fork ran and passed:
  - `cargo fmt --all -- --check`
  - `cargo clippy --all-targets --all-features -- -D warnings`

Files changed/artifacts produced:

- Build: `src/bin/x0xd.rs` only.
- Planning: this checkpoint.

Checks run by orchestrator before commit/push:

- `cargo fmt --all` — passed.
- `cargo clippy --all-features --all-targets -- -D warnings` — passed.
- `cargo check --workspace --all-targets` — passed.
- `cargo test --all-features --bin x0xd withdrawn_card` — passed, 6 tests.
- `cargo test --all-features --bin x0xd treekem_journal_replay` — passed, 3 tests.
- `cargo test --all-features --bin x0xd treekem_persistence_drop_removes_snapshot_and_journal` — passed, 1 test.
- `cargo test --all-features --bin x0xd withdrawn` — passed, 18 tests.
- `cargo test --all-features --bin x0xd lost_race` — passed, 19 tests.
- `cargo test --all-features --bin x0xd atomic_persist` — passed, 1 test.
- `cargo test --all-features --bin x0xd persistence_drop` — passed, 1 test.

Additional review/clean-context checks:

- Code reviewer reran `cargo fmt --all -- --check`, `cargo clippy --all-features --all-targets -- -D warnings`, `cargo check --workspace --all-targets`, and `cargo test --all-features --bin x0xd withdrawn_card_protected_crypto_probe_fails_closed_on_io_error` — all passed.
- Clean-context reran `cargo test --all-features --bin x0xd withdrawn_card_protected_crypto_probe_fails_closed_on_io_error`, `cargo fmt --all -- --check`, `cargo check --workspace --all-targets`, and `cargo clippy --all-features --all-targets -- -D warnings` — all passed.

## Honesty rules check

- No-harness-modification: Pass.
  - No harness, CI, gate, wrapper, dependency, build invocation, wire/format, public API, or startup/health/networking changes.
- Baseline-diff for evidence: Pass with existing Phase 1 baseline record.
  - CI reds match already base-reproduced startup-timeout failures; this polish has a clean diff guard and is no plausible cause.
- Evidence reproducible-from-branch: Pass for build/code evidence.
  - CI carve-out records live in the planning branch and must be carried into any maintainer handoff.
- Local vs CI consistency: Pass with caveat.
  - Local focused checks pass; CI raw red remains only the known daemon startup-health timeout class.

## Review findings

Code review:

- Reviewer/tool: `codereviewer`.
- Result: passed.
- Findings: none. Reviewer confirmed `Ok(false)` remains false, IO errors now fail closed, test is meaningful, dedup does not hold mutex across await, and no wire/format/happy-path change exists.

Verifier:

- Reviewer/tool: `verifier`.
- Result: passed, 6/6 goals.
- Findings: none. Scope/stop rules held.

Craft Review:

- Reviewer/tool: `craft` (`gpt-5.5`; implementer model/provider unknown, so independence weaker than confirmed cross-provider).
- Verdict: PASS.
- CONFORMANCE findings and dispositions: none.
- SIMPLICITY / NIT findings carried: none.

Adversarial review:

- Reviewer/tool: `adversarial` (`gpt-5.5`; implementer model/provider unknown, so independence weaker than confirmed cross-provider).
- Result: READY-WITH-NITS.
- Blocking findings: none.
- LOW carried: log text at the withdrawn-card import caller says `ignored withdrawn card for live keyed group; signed withdrawal commit required`, while `protects_keyed_local_group == true` can now also mean unreadable/probe-error protected material. Disposition: accepted as non-blocking; do not change code in this polish pass.
- NIT carried: new IO-error regression directly exercises the snapshot probe error path; journal branch fail-closed behavior is covered by symmetric code review, not an independent test. Disposition: accepted as non-blocking.

Clean-context test:

- Reviewer/tool: `cleancontext`.
- Result: Pass.
- Findings: no blockers or concerns. Clean-context confirmed the hardening is understandable from source/diff/formal artifacts, `Err` fails closed while `Ok(false)` remains false, comments do not mislead, dedup is test-local, and CI wording avoids overstating raw red as green.

## Drift / scope concerns

- None introduced by the polish.
- Existing Phase 2 deferral remains: card-bound discovery/request-access authority seeding, recorded in PR notes.
- Existing raw-red CI caveat remains: internal acceptance only under startup-timeout carve-out.

## Open questions / decisions for Jim

PR / upstream action gate:

- PR ready to raise? Not automatically by this checkpoint. Build branch is ready for Jim's PR decision under recorded deferrals/carve-out.
- Jim confirmed PR may be opened? No.
- Draft PR title/description prepared: running notes exist in `gsd/plan/phase-1-pr-notes.md`; final PR body not prepared here.

## Recommended next step

If proceeding toward upstream PR, prepare final PR description with:

- Phase 2 deferrals: delegated ban / KeyPackage distribution and card-bound discovery/request-access authority seeding.
- Raw-red/internal-carve-out CI status; do not claim raw CI green.
- Final polish note if desired: withdrawn-card protection now fails closed on unreadable TreeKEM snapshot/journal probes.

Then ask Jim for explicit PR-open approval.

## Handoff note

Phase 1 build branch is feature-complete at `330965f411f17ab328c68ca9b39dfa46bfa6d5b5`. The final polish is committed and pushed to Jim fork only. Reviews and clean-context passed with only non-blocking adversarial LOW/NIT. PR #5 is raw red but internally accepted under the documented startup-timeout carve-out.
