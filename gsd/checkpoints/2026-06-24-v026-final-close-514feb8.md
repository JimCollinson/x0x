# GSD Checkpoint — ADR-0016 Phase 1 v0.26 final close

Date: 2026-06-24
Project: x0x
Slice/question: ADR-0016 Phase 1 v0.26 final close after terminality/adversarial findings
Prepared by: orchestrator
Agents/tools used: operative, codereviewer, verifier, cleancontext, adversarial, craft

## Status

Done for fork-only final close; ready for Jim's review/PR decision gate.

Meaningful work-unit? Yes — non-trivial group-authority/security behavior intended for upstream PR.
Review cadence: per-unit/final integrated gauntlet completed. No unreviewed backlog.

Note: no upstream PR has been opened or approved. PR #6 remains a fork-only draft CI mirror and must not be merged.

## What happened

- Jim/Cowork final-close instructions were applied:
  1. `validate_apply` now rejects live→withdrawn by default and exposes explicit terminal validation for `GroupDeleted`.
  2. `seal_withdrawal` now leaves `GroupInfo` keyless on successful withdrawal while preserving auth-before-mutation and failure atomicity.
  3. `Moderator`/`Guest` signed apply was kept unchanged and recorded as accepted reserved-role rationale; authoring APIs still reject reserved roles.
- Implementation commits added on `feat/adr-0016-phase-1-on-v0.26`:
  - `7225ecfe4e6c98edc0d60f99f1d719bbf4ed9103 fix(adr-0016): require explicit terminal validation`
  - `514feb8319bbed2acb0403f3f3a90de1321ecae3 docs(adr-0016): use durable reserved-role comments`
- PR #6 body was updated to name final head `514feb8319bbed2acb0403f3f3a90de1321ecae3`.

## Evidence

CI arbiter / green of record:

- Location: PR #6, <https://github.com/JimCollinson/x0x/pull/6>
- Status source: GitHub checks on draft fork-only mirror PR.
- Final head: `514feb8319bbed2acb0403f3f3a90de1321ecae3`
- Status: all substantive checks pass; `Soak Test` skipped by workflow.
- Check summary at final head:
  - API + GUI Parity Gate — pass
  - API Coverage Guard — pass
  - Build linux-arm64-gnu — pass
  - Build linux-x64-gnu — pass
  - Build linux-x64-musl — pass
  - Build macos-arm64 — pass
  - Build macos-x64 — pass
  - Build windows-x64 — pass
  - Cargo Audit — pass
  - Clippy Lint — pass
  - Coverage Gate — pass
  - Documentation — pass
  - Format Check — pass
  - Multi-Agent Integration — pass
  - Panic Scanner — pass
  - Property Tests — pass
  - Test Suite — pass
  - Validate release metadata — pass
  - Soak Test — skipped

Local fast gate / `.gsd/gate.sh`:

- Installed? N/A — no `.gsd/gate.sh` exists in the implementation worktree.
- Mandatory Rust checks run after code changes:
  - `cargo fmt --all` — pass
  - `cargo clippy --all-features --all-targets -- -D warnings` — pass
  - `cargo check --workspace --all-targets` — pass
- After comment-only Craft remediation, mandatory Rust checks were rerun in order:
  - `cargo fmt --all` — pass
  - `cargo clippy --all-features --all-targets -- -D warnings` — pass
  - `cargo check --workspace --all-targets` — pass

Files changed/artifacts produced:

- Implementation final-close files:
  - `src/groups/state_commit.rs`
  - `src/groups/mod.rs`
  - `src/server/mod.rs`
  - `tests/named_group_state_commit.rs`
  - `tests/membership_authority.rs`
  - `tests/proptest_groups.rs`
  - `tests/last_admin_invariant.rs`
  - `tests/owner_retirement.rs`
- Planning:
  - `gsd/checkpoints/2026-06-24-v026-adversarial-blocker-ba03f38.md`
  - this checkpoint

Checks run:

- Operative:
  - `cargo fmt --all` — pass
  - `cargo clippy --all-features --all-targets -- -D warnings` — pass
  - `cargo check --workspace --all-targets` — pass
  - `cargo nextest run --all-features --test named_group_state_commit` — pass, 24/24
  - `cargo test --all-features metadata_` — pass
  - `cargo nextest run --all-features --test membership_authority` — pass, 25/25
  - `cargo nextest run --all-features --test proptest_groups` — pass, 11/11
  - `cargo nextest run --all-features --test last_admin_invariant` — pass, 12/12
  - `cargo nextest run --all-features --test owner_retirement` — pass, 8/8
  - `cargo test --all-features validate_apply_` — pass, 9/9
  - `cargo test --all-features seal_withdrawal_` — pass, 2/2
  - `git diff --check` — pass
- Code reviewer reran:
  - `cargo fmt --all -- --check` — pass
  - `cargo clippy --all-features --all-targets -- -D warnings` — pass
  - `cargo check --workspace --all-targets` — pass
  - focused tests listed above — pass
- Verifier ran targeted final-close checks and passed 7/7.
- Clean-context final gate ran focused local tests at final head, including raw validation, terminal validation, `seal_withdrawal`, reserved-role, owner assignment rejection, admin `GroupDeleted`, and last-admin withdrawal tests — all pass.

## Honesty rules check

- No-harness-modification: Pass. Final-close diff from `ba03f38` touched only group/server source and focused tests. No `.github`, `.gsd`, `tests/harness`, `Cargo.toml`, `Cargo.lock`, dependency, networking, bootstrap, presence, wire/hash/signing/storage-format changes.
- Baseline-diff for evidence: Pass / N/A. No local failure or skip was used to justify readiness. CI was green at final head; no carve-out needed.
- Evidence reproducible-from-branch: Pass for implementation/CI. Formal GSD planning artifacts live in the planning worktree; this checkpoint records exact final head/evidence.
- Local vs CI consistency: Pass. Focused local tests and CI are green.

## Review findings

Clean-context test:

- Reviewer/tool: cleancontext
- Result: Concerns, with no blocker.
- Findings:
  - Behaviour is discoverable from committed source/docs/tests and formal planning artifacts.
  - Verified exact final PR #6 head `514feb8319bbed2acb0403f3f3a90de1321ecae3`, all substantive CI checks pass, `Soak Test` skipped.
  - Verified final-close behaviours: default `validate_apply` live→withdrawn rejection; explicit terminal validation; successful `seal_withdrawal` key wipe; failure atomicity; reserved-role authoring rejection and signed-apply rationale.
  - Only concern was absence of a final checkpoint at exact head before this file. This checkpoint resolves that process/current-state concern.

Adversarial review:

- Reviewer/tool: adversarial (`openai/gpt-5.5`; implementer model/provider not provided, so provider diversity cannot be proven beyond fresh-context review)
- Required? Yes — meaningful upstream/security-relevant work.
- Result: READY for the three final-close items.
- Findings:
  - No CRITICAL/HIGH/MEDIUM/LOW/NIT findings for final-close items.
  - Explicitly did not re-open the settled `Moderator`/`Guest` signed-apply decision.
  - Verified authoring still rejects reserved roles and signed-apply rationale is recorded.
  - Note: adversarial ran at functional head `7225ecfe4e6c98edc0d60f99f1d719bbf4ed9103`; subsequent final commit `514feb8319bbed2acb0403f3f3a90de1321ecae3` is comment-only Craft remediation and was covered by Craft follow-up plus final CI.

Craft Review:

- Reviewer/tool: craft
- Required? Yes — meaningful upstream/maintainer-sensitive work.
- Verdict: Pass after remediation.
- Findings and disposition:
  - CONFORMANCE: process wording `final-close` leaked into durable source/test comments. Disposition: fixed in `514feb8319bbed2acb0403f3f3a90de1321ecae3` by using durable `ADR-0016 reserved-role rationale` wording.
  - NIT: server comment named old `validate_apply`; disposition: fixed to `terminal apply validation` wording.
  - Craft follow-up at final head: Pass; prior CONFORMANCE finding closed; no remaining CONFORMANCE/SIMPLICITY/NIT findings in narrow scope.

Code review:

- Reviewer/tool: codereviewer
- Result: passed
- Notes: no blocking issues; no production panic/unwrap/expect concern introduced; no Cargo/dependency/CI/gate/harness or accepted-ADR changes detected.

Verification:

- Reviewer/tool: verifier
- Result: passed
- Score: 7/7 goals verified
- Goals verified:
  1. end-to-end metadata dispatch rejects non-`GroupDeleted` `withdrawn=true` while live;
  2. raw/default `validate_apply` rejects live→withdrawn without terminal context;
  3. explicit terminal validation/finalization used by server `GroupDeleted` and requires Admin + withdrawn commit;
  4. key material wiped on successful `seal_withdrawal` and terminal finalization;
  5. reserved-role signed-apply unchanged, rationale recorded, no authority granted to Moderator/Guest;
  6. no wire/hash/signing/storage/dependency/CI/harness changes; Accepted ADR unchanged;
  7. tests meaningfully cover final-close behaviours.

## Drift / scope concerns

- None blocking.
- PR #6 remains draft/fork-only/do-not-merge.
- No upstream PR has been opened; Jim's explicit approval remains required for any upstream PR action.

## Open questions / decisions for Jim

- None for final-close implementation.

PR / upstream action gate:

- PR ready to raise? Technically ready for Jim's review/decision based on final-close gates, but no upstream PR may be opened without Jim's explicit confirmation.
- Jim confirmed upstream PR may be opened? No.
- Draft PR title/description prepared: planning draft exists; PR #6 is only the fork CI mirror.

## Recommended next step

Return to Jim/Cowork for final review and explicit upstream PR decision.

## Handoff note

ADR-0016 Phase 1 v0.26 final-close work is complete on the fork branch at `514feb8319bbed2acb0403f3f3a90de1321ecae3`. PR #6 is green at that head. The final terminality concerns are closed: default validation rejects live→withdrawn, explicit terminal validation handles admin `GroupDeleted`, successful withdrawal clears key material, and reserved-role signed-apply remains intentionally accepted for convergence with durable rationale. No upstream PR has been opened.
